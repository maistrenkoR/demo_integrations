describe("template spec", () => {
  beforeEach(() => {
    cy.visit("https://example.cypress.io");
  });
  it("Does not do much!", () => {
    expect(true).to.equal(true);
  });
  it("Does not do much!", () => {
    expect(true).to.equal(true);
  });
});
